spring:
  application:
    name: prefab.kafka-example

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_OAUTH2_CLIENT_ID}
            client-secret: ${GOOGLE_OAUTH2_CLIENT_SECRET}

topics:
  channel.name: prefab.kafka-example.channel
  message.name: prefab.kafka-example.message
  user.name: prefab.kafka-example.user
