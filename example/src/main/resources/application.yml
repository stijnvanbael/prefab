spring:
  application:
    name: example
  cloud:
    gcp:
      project-id: example

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/prefab_example
    username: example
    password: example

kafka:
  topics:
    sale:
      name: sale


logging.level:
  root: WARN
  be.appify.prefab.example: DEBUG