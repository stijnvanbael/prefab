package rest.override.application;

/**
 * Manually written service that overrides the generated WidgetService.
 * This class is intentionally placed in the application package to test that
 * the annotation processor emits a NOTE when it detects an existing source file.
 */
public class WidgetService {
    // Manual override: custom business logic here
}
