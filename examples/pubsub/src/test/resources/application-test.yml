spring:
  flyway:
    clean-disabled: false

logging.level:
  be.appify.prefab.example: DEBUG

prefab:
  flyway:
    auto-clean-on-mismatch: true
  dlt:
    retries:
      initial-interval-ms: 10
      multiplier: 2
  outbox:
    poll-interval-ms: 100
  test:
    schema-registry:
      enabled: true
