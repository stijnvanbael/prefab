spring:
  flyway:
    clean-disabled: false

logging.level:
  be.appify.prefab.example: DEBUG

prefab:
  dlt:
    retries:
      limit: 3
      initial-interval-ms: 10
  outbox:
    poll-interval-ms: 500
    batch-size: 10