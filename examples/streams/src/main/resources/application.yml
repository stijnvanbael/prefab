spring:
  application:
    name: streams-example
  kafka:
    streams:
      application-id: streams-example-app
      properties:
        auto.offset.reset: earliest

topics:
  streams:
    input: streams.input
    words: streams.words
    short-words: streams.words.short
    long-words: streams.words.long
    join-left: streams.join.left
    join-right: streams.join.right
    joined: streams.joined

