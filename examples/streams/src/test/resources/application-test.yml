spring:
  kafka:
    streams:
      application-id: streams-example-test
      properties:
        auto.offset.reset: earliest

topics:
  streams:
    input: streams.input.test
    words: streams.words.test
    short-words: streams.words.short.test
    long-words: streams.words.long.test
    join-left: streams.join.left.test
    join-right: streams.join.right.test
    joined: streams.joined.test
